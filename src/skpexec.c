#include "skpopcodes.h"
#include "skp.h"

typedef struct {
  uint16_t store;
  uint16_t start;
  uint16_t capt[0];
} execlvl_t;

int skpexec(char *prg, char *s)
{
  int len = -1;

  

  return len;
}
